<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.groupware.core.domain.schedule.mapper.ScheduleMapper">

    <!-- 일정 저장 (예시) -->
    <insert id="insertSchedule" parameterType="ScheduleEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO schedule (
            user_id,
            title,
            description,
            location,
            start_date,
            end_date,
            all_day,
            schedule_type,
            reminder_minutes,
            color,
            is_public,
            created_at,
            created_by
        ) VALUES (
            #{userId},
            #{title},
            #{description},
            #{location},
            #{startDate},
            #{endDate},
            #{allDay},
            #{scheduleType},
            #{reminderMinutes},
            #{color},
            #{isPublic},
            NOW(),
            #{createdBy}
        )
    </insert>

</mapper>